
//system

//laboratorio

const rootTypeDefs = `#graphql
    type Query {
        _: String
    }
    
    type Mutation {
        _: String
    }
`
const resolvers: [] = [
]
const typeDefs = [
    rootTypeDefs,
]

export { typeDefs, resolvers }